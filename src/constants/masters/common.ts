// Shared sentinel + tiny primitives reused across every master domain.

/** The universal "Other" option appended to pickers that allow a custom value. */
export const OTHER_OPTION = "Other" as const;

export const YES_NO = ["Yes", "No"] as const;
